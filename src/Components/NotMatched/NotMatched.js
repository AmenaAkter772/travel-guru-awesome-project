import React from 'react';

const NotMatched = () => {
    return (
        <div>
            <h1 style={{color:'white', textAlign:'center', marginTop:'100px'}}>Error !!! 404 . Sorry the search result not matched</h1>
        </div>
    );
};

export default NotMatched;